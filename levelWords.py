import pygame
from includeFont import fontText

class levelWords:
    def levelOneWords(index):
        wordList = ["hello", "world", "pizza", "pasta", "hotdog", "skiing"]
        return wordList[index]
